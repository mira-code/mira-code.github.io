$(function () {
    // $('a.js-scroll-trigger[href*="#"]:not([href="#"])').click(function (e) {
    //     if (location.pathname.replace(/^\//, "") == this.pathname.replace(/^\//, "") && location.hostname == this.hostname) {
    //         e.preventDefault();

    //         var a = $(this.hash);
    //         if ((a = a.length ? a : $("[name=" + this.hash.slice(1) + "]")).length) {
    //             return $("html, body").stop().animate({
    //                 scrollTop: a.offset().top - 56
    //             }, 1e3, "easeInOutExpo");
    //         }
    //     }
    // });

    /*$('#sendRequest').submit(function (e) {
        e.preventDefault();
        
        var name = $('#sendRequestName').val(),
            phone = $('#sendRequestPhone').val();
        
        Email.send(
            "denisbednov@outlook.com",
            "denisbednov@outlook.com",
            "Miracode - Заявка на разработку сайта",
            "Имя: " + name + ", телефон: " + phone,
            "smtp.elasticemail.com",
            "denisbednov@outlook.com",
            "9ed9b957-5220-453b-bf1f-8ece16d9dc91",
            function (message) {
                alert('Ваш запрос отправлен. Спасибо за обращение.');
        });
    });*/
});